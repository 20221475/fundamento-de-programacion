#include <stdio.h>
/* promedio de curso
si el promedio es igual o mayor que 6 aprobado

pro: variable de tipo real. */
int main(void) {
  float pro;
  printf("ingresa el promedio del alumno\n");
  scanf("%f", &pro);
    if (pro >=6);
}
